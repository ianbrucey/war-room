-- =============================================================================
-- DATABASE SCHEMA - Zero Ambiguity Artifact
-- =============================================================================
-- Generated From: 00-Brief.md
-- Purpose: Define ALL database structures before any code is written
-- Rule: Builders CANNOT add columns. Schema changes require Council approval.
-- =============================================================================

-- -----------------------------------------------------------------------------
-- TABLES
-- -----------------------------------------------------------------------------

-- Example structure (replace with actual schema):
-- CREATE TABLE table_name (
--     id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
--     column_name TYPE NOT NULL,
--     created_at TIMESTAMP WITH TIME ZONE DEFAULT NOW(),
--     updated_at TIMESTAMP WITH TIME ZONE DEFAULT NOW()
-- );

-- -----------------------------------------------------------------------------
-- INDEXES
-- -----------------------------------------------------------------------------

-- -----------------------------------------------------------------------------
-- FOREIGN KEYS
-- -----------------------------------------------------------------------------

-- -----------------------------------------------------------------------------
-- TRIGGERS
-- -----------------------------------------------------------------------------

-- =============================================================================
-- VALIDATION CHECKLIST
-- =============================================================================
-- [ ] All column types are explicit (no ambiguous types)
-- [ ] All NOT NULL constraints are intentional
-- [ ] All foreign keys are defined
-- [ ] Indexes exist for frequently queried columns
-- [ ] Schema matches the Claims in 00-Brief.md
-- =============================================================================

